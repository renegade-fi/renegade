[package]
name = "util"
version = "0.1.0"
edition = "2021"

[dependencies]
# === Networking === #
libp2p = { workspace = true }

# === Runtime === #
futures = "0.3"
tokio = { workspace = true }

# === Misc === #
chrono = "0.4"
env_logger = "0.9"
eyre = { workspace = true }
json = "0.12"
tracing = { version = "0.1", features = ["log"] }
tracing-subscriber = "0.3"
